#include <iostream>
#include <fstream>
#include <string>
#include <math.h>

using namespace std;

int main() {
    // Change the file path to the path of the file you want to read

    double sum = 0;

    string line;
    while (getline(cin , line)) {
        if ()
    }
        cout << "Total Profit : " << to_string(sum) << ".";


    return 0;
}